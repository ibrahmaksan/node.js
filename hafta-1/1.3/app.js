const topla = require('./utils.js') // modülümüzü import ettik.
const toplam = topla(4, -2) // topla degiskeni artik toplama fonksiyonu oldugundan bu şekilde işlem yapabiliyoruz. 
console.log(toplam) // ekrana toplamı basar.